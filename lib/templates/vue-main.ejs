import { createApp } from "vue";
import App from "./App.vue";
import './assets/css/global.css';
<% if ( deploy.Pinia) { %>import "normalize.css/normalize.css";<% } %>
<% if ( deploy.Pinia) { %>import { createPinia } from "pinia";<% } %>
<% if ( deploy.Vuex) { %>import store from '@/store/index.js'<% } %>
<% if (deploy.router) {%>import router from '@/router/index.js'<% } %>
let app = createApp(App)
app<% if ( deploy.router) { %>.use(<%= deploy.router %>)<% } %><% if ( deploy.Pinia) { %>.use(createPinia())<% } %><% if ( deploy.Vuex) { %>.use(store)<% } %>.mount("#app");


