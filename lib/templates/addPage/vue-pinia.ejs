import { defineStore } from 'pinia'

export const use<%= name %>Store = defineStore('<%= lowerName %>', {
    id:'<%= lowerName %>',
      state: () => {
      },
      computed: {
      },
      actions: {
      },
      })